#matmul.py
import torch

device = 'cuda' else 'cpu'
A = torch.rand(100,100).to(device)
B = torch.rand(100,100)
##
for i in range(3):
  A @ B


#here is how to implement k means clustering
sklearn.model_selection
